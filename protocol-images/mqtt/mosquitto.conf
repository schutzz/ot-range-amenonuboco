# Mosquitto 設定ファイル（amenonuboco 用最小設定）
# TLS の有効化は run.py が動的に書き換える（TLS_ENABLE=true 時）。

# 平文 MQTT リスナー (1883)
listener 1883
allow_anonymous true

# ログを stdout に出す（コンテナでの確認用）
log_dest stdout
log_type all
