apiVersion: v1
kind: CyberRange
metadata:
  name: generic-observability-excluded
topology:
  segments:
    - name: control
      cidr: 10.10.0.0/24
      kind: ot-lan
    - name: observation
      cidr: 10.10.1.0/24
      kind: observation
  assets: []
instrumentation:
  mirror_to: observation
  exclude:
    - control
observability_contract:
  required_segments:
    - control
